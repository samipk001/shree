Place original brand logo image files here (use PNG or SVG). The site expects these filenames:

- ingco.png   -> INGCO original logo (use full logo with transparent background)
- libang.png  -> Libang original logo (higher-resolution; will be upscaled)
- boda.png    -> Boda brand: place an image where the actual logo mark is at the upper-left; the stylesheet will crop/upscale the left-top area.

Notes:
- For best results use transparent PNGs or SVGs at 2x resolution (e.g., 600–1200px wide) so the site can upscale without artifacts.
- If you need me to import the attached images into this folder, upload them to the workspace (or tell me where they are) and I can add them automatically.
